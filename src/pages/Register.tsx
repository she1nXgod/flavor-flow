import Layout from '../components/Layout';
import RegisterForm from '../components/RegisterForm';

const Register = () => {
  return (
    <Layout background='register-bg'>
      <RegisterForm />
    </Layout>
  );
};

export default Register;
